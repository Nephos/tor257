#encoding: utf-8

task :default => [:test]

desc "Unitary tests. Fasts and on the sources."
task :test do
  ruby "test/test.rb"
end

desc "Benchmark (usefull to test complexity)"
task :benchmark do
  ruby "test/benchmark.rb"
end
